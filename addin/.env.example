# 前端只持短期会话 token，模型 key 永不出现在前端（架构铁律 2）。
# 复制为 .env 本地使用；.env 已被 .gitignore 忽略。

# SSE 客户端基址；开发期 Vite 代理 /api → BFF。
VITE_API_BASE=/api

# 开发期会话 token：从环境注入，禁止用 localStorage 持久化（架构铁律 7）。
# 生产签发方式属 TODO（见 CLAUDE_1.md），补全前不要硬编码真实 token。
VITE_DEV_SESSION_TOKEN=dev-token
